logging.level.org.springframework: INFO

management:
  endpoints:
    web:
      # e.g. http://localhost:8080/actuator/gateway/routes
      exposure:
        include: gateway

spring:
  profiles:
    include: 'price-list'

payper:
  baseUrl: 'http://localhost:8080'
  redirectPath: 'pypr/payment-required'